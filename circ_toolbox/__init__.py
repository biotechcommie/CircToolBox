# circ_toolbox_project/circ_toolbox/__init__.py
# This file serves to mark circ_toolbox as a package
# No additional logic is needed unless specific package-wide initializations are required.

'''
import os
import sys

# Set the project root to the grandparent directory of this file
PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))

# Add project root to the system path if not already there
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)

# Ensure the circ_toolbox package can be imported properly
PACKAGE_ROOT = os.path.abspath(os.path.dirname(__file__))
if PACKAGE_ROOT not in sys.path:
    sys.path.insert(0, PACKAGE_ROOT)

# Optional: Log initialization for debugging purposes (remove if not needed)
# print(f"circ_toolbox initialized, PROJECT_ROOT: {PROJECT_ROOT}")

'''
